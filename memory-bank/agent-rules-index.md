# Agent Rules Index

> Auto-generated by `/banyan-rules-index`. Lists all agent rule files with their matching criteria so sub-agents can load only relevant rules.

**Last generated**: 2026-06-21
**Total rule files**: 7 (7 learned, 0 user-authored)

## Rules

| Rule | Priority | Globs | Paths | Topics | Source |
|------|----------|-------|-------|--------|--------|
| Learned: Error Handling | low | `src/middleware/errorHandler.ts`, `src/**/*.ts` | — | error-handling, express, security | `_learned/error-handling.md` |
| Learned: Testing Patterns | medium | `**/*.test.ts`, `*.test.ts`, `src/db/**`, `src/routes/**`, `client/e2e/**`, `**/*.spec.ts` | — | testing, jest, logging, mocking, e2e, playwright, realtime | `_learned/testing-patterns.md` |
| Learned: TypeScript Config | low | `tsconfig.json`, `src/config/**/*.ts` | — | typescript, config, 12-factor | `_learned/typescript-config.md` |
| Learned: API Design | medium | `src/app.ts`, `src/index.ts`, `src/middleware/**/*.ts`, `src/routes/**/*.ts`, `src/db/**/*.ts` | — | api-design, express, testability, routing | `_learned/api-design.md` |
| Learned: Tooling Constraints | low | `**/.env*`, `docker-compose.yml`, `**/package.json` | — | tooling, env-files, claude-code-settings, npm, monorepo | `_learned/tooling.md` |
| Learned: Frontend (React) Patterns | medium | `client/src/**/*.tsx`, `client/src/**/*.ts` | — | frontend, react, data-fetching, error-handling, realtime | `_learned/frontend.md` |
| Learned: Real-Time Transport Patterns | low | `src/realtime/**`, `client/src/realtime/**` | — | realtime, sse, transport, backend, architecture | `_learned/realtime.md` |

## Validation

- **Context overload**: None detected. Heaviest single-file match is a non-test file under `src/routes/` (e.g. `src/routes/cards.ts`): Error Handling (`src/**/*.ts`) + API Design (`src/routes/**/*.ts`) + Testing Patterns (`src/routes/**`) = 3 rules, ~80 lines — well under the 300-line warning threshold. Frontend files under `client/src/` match the Frontend rule (1 rule); `client/src/realtime/**` additionally matches the Real-Time Transport rule (2 rules). Backend `src/realtime/**` matches Error Handling + Real-Time Transport (2 rules).
- **Conflicts**: None detected. Rules are additive across distinct concerns (error handling, testing, config, API composition, tooling, frontend, real-time transport). The Frontend rule's `error-handling` topic complements (does not contradict) the backend Error Handling rule — they apply to disjoint glob sets (`client/src/**` vs `src/**`). The Real-Time Transport rule complements both Frontend and API Design on disjoint `realtime/` glob subtrees.
- **Unsafe rules**: None detected (all rules are dev-scoped directives derived from task reflections).

## Notes

All current rules are auto-generated learnings extracted from task reflections (TASK-001 through TASK-007). `Testing Patterns`, `API Design`, and `Frontend (React) Patterns` are at `medium` priority (each reinforced across 3+ tasks; `Frontend` promoted during the TASK-007 archive at evidence_count 4). TASK-007 added the first `Real-Time Transport Patterns` rule (low, evidence 1) as the project gained its first real-time tier, and extended `Frontend (React) Patterns` (EventSource useRef + unmount-close; `X-Client-Id`→`originId` echo de-dup) and `Testing Patterns` (dedicated real-DB Playwright project for cross-context SSE/WebSocket). The remaining rules are `low` priority and will not override human-authored rules. See `memory-bank/learning-log.md` for provenance.
