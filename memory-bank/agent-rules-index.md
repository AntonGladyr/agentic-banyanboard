# Agent Rules Index

> Auto-generated by `/banyan-rules-index`. Lists all agent rule files with their matching criteria so sub-agents can load only relevant rules.

**Last generated**: 2026-06-21
**Total rule files**: 6 (6 learned, 0 user-authored)

## Rules

| Rule | Priority | Globs | Paths | Topics | Source |
|------|----------|-------|-------|--------|--------|
| Learned: Error Handling | low | `src/middleware/errorHandler.ts`, `src/**/*.ts` | — | error-handling, express, security | `_learned/error-handling.md` |
| Learned: Testing Patterns | medium | `**/*.test.ts`, `*.test.ts`, `src/db/**`, `src/routes/**`, `client/e2e/**`, `**/*.spec.ts` | — | testing, jest, logging, mocking, e2e, playwright | `_learned/testing-patterns.md` |
| Learned: TypeScript Config | low | `tsconfig.json`, `src/config/**/*.ts` | — | typescript, config, 12-factor | `_learned/typescript-config.md` |
| Learned: API Design | medium | `src/app.ts`, `src/index.ts`, `src/middleware/**/*.ts`, `src/routes/**/*.ts`, `src/db/**/*.ts` | — | api-design, express, testability, routing | `_learned/api-design.md` |
| Learned: Tooling Constraints | low | `**/.env*`, `docker-compose.yml`, `**/package.json` | — | tooling, env-files, claude-code-settings, npm, monorepo | `_learned/tooling.md` |
| Learned: Frontend (React) Patterns | low | `client/src/**/*.tsx`, `client/src/**/*.ts` | — | frontend, react, data-fetching, error-handling | `_learned/frontend.md` |

## Validation

- **Context overload**: None detected. Heaviest single-file match is a non-test file under `src/routes/` (e.g. `src/routes/cards.ts`): Error Handling (`src/**/*.ts`) + API Design (`src/routes/**/*.ts`) + Testing Patterns (`src/routes/**`) = 3 rules, ~80 lines — well under the 300-line warning threshold. Frontend files under `client/src/` match only the Frontend rule (1 rule).
- **Conflicts**: None detected. Rules are additive across distinct concerns (error handling, testing, config, API composition, tooling, frontend). The Frontend rule's `error-handling` topic complements (does not contradict) the backend Error Handling rule — they apply to disjoint glob sets (`client/src/**` vs `src/**`).
- **Unsafe rules**: None detected (all rules are dev-scoped directives derived from task reflections).

## Notes

All current rules are auto-generated learnings extracted from task reflections (TASK-001 through TASK-006). `Testing Patterns` and `API Design` are at `medium` priority (each reinforced across 3+ tasks). TASK-006 added the first `Frontend (React) Patterns` rule (low, evidence 2) as the project gained its first frontend tier, and extended `Tooling Constraints` (npm subpackage install) and `Testing Patterns` (hermetic Playwright E2E). The remaining rules are `low` priority and will not override human-authored rules. See `memory-bank/learning-log.md` for provenance.
