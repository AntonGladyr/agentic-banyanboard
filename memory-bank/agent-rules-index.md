# Agent Rules Index

> Auto-generated by `/banyan-rules-index`. Lists all agent rule files with their matching criteria so sub-agents can load only relevant rules.

**Last generated**: 2026-06-17
**Total rule files**: 5 (5 learned, 0 user-authored)

## Rules

| Rule | Priority | Globs | Paths | Topics | Source |
|------|----------|-------|-------|--------|--------|
| Learned: Error Handling | low | `src/middleware/errorHandler.ts`, `src/**/*.ts` | — | error-handling, express, security | `_learned/error-handling.md` |
| Learned: Testing Patterns | medium | `**/*.test.ts`, `*.test.ts`, `src/db/**`, `src/routes/**` | — | testing, jest, logging, mocking | `_learned/testing-patterns.md` |
| Learned: TypeScript Config | low | `tsconfig.json`, `src/config/**/*.ts` | — | typescript, config, 12-factor | `_learned/typescript-config.md` |
| Learned: API Design | low | `src/app.ts`, `src/index.ts`, `src/middleware/**/*.ts`, `src/routes/**/*.ts` | — | api-design, express, testability | `_learned/api-design.md` |
| Learned: Tooling Constraints | low | `**/.env*`, `docker-compose.yml` | — | tooling, env-files, claude-code-settings | `_learned/tooling.md` |

## Validation

- **Context overload**: None detected. Heaviest single-file match is a non-test file under `src/routes/` (e.g. `src/routes/boards.ts`): Error Handling (`src/**/*.ts`) + API Design (`src/routes/**/*.ts`) + Testing Patterns (`src/routes/**`) = 3 rules, ~68 lines — well under the 300-line warning threshold.
- **Conflicts**: None detected. Rules are additive across distinct concerns (error handling, testing, config, API composition, tooling).
- **Unsafe rules**: None detected (all rules are dev-scoped directives derived from task reflections).

## Notes

All current rules are auto-generated learnings extracted from task reflections (TASK-001 through TASK-004). `Testing Patterns` has been promoted to `medium` priority (reinforced across 4 tasks). The remaining rules are `low` priority and will not override human-authored rules. See `memory-bank/learning-log.md` for provenance.
